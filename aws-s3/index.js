const config = require('aws-s3/config');
const actions = require('aws-s3/actions');
const keys = require('aws-s3/keys');
const ext = require('aws-s3/extensions')

module.exports = { config, actions };